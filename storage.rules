rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow all authenticated requests since we handle authentication in our app
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
